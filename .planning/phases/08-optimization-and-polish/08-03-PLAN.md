---
phase: 08-optimization-and-polish
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - src/components/hero/VideoBackground.tsx
  - src/components/hero/HeroFallback.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Build passes with zero TypeScript errors after all Phase 8 changes"
    - "No console.log/warn/error statements in shipped source"
    - "No TODO or FIXME comments in shipped hero source"
    - "Safari webkit prefixes are present for backdrop-filter"
    - "Video elements include WebM source for Firefox optimization (if WebM file exists)"
    - "Hero renders without visual defects at 375px, 768px, 1024px, 1440px viewports"
  artifacts:
    - path: "src/components/hero/VideoBackground.tsx"
      provides: "WebM source tag (if applicable)"
    - path: "src/components/hero/HeroFallback.tsx"
      provides: "WebM source tag (if applicable)"
  key_links:
    - from: "src/app/globals.css"
      to: "Glass panel components"
      via: "-webkit-backdrop-filter prefix"
      pattern: "webkit-backdrop-filter"
---

<objective>
Final QA pass: verify cross-browser compatibility, add WebM video sources if available, audit for leaked console statements and TODO markers, and visually validate the hero at four viewport widths (375px, 768px, 1024px, 1440px).

Purpose: Ship-quality gate. This plan runs after Wave 1 optimizations (compressed GLB, lazy-loaded ProductIntro, cleaned HeroScene) and validates the final product is ready for deployment — no broken rendering, no debug artifacts, no cross-browser surprises.

Output: Clean, verified codebase ready for `/gsd-verify-work`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-optimization-and-polish/08-RESEARCH.md
@.planning/phases/08-optimization-and-polish/08-01-SUMMARY.md (GLB compression + lazy load results)
@.planning/phases/08-optimization-and-polish/08-02-SUMMARY.md (dead code removal + architecture verification)

@src/app/globals.css (webkit prefix check)
@src/components/hero/VideoBackground.tsx
@src/components/hero/HeroFallback.tsx
@src/components/hero/HeroSection.tsx
@src/components/hero/HeroMobile.tsx
@src/components/hero/HeroCTA.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-browser code audit and WebM video source</name>
  <files>src/components/hero/VideoBackground.tsx, src/components/hero/HeroFallback.tsx</files>
  <action>
1. **Verify Safari webkit prefixes in globals.css:**
   Run: `grep -n "webkit-backdrop-filter" src/app/globals.css`
   Expected: `-webkit-backdrop-filter` present alongside every `backdrop-filter` declaration.
   Status from pre-investigation: Already present on lines 83, 140, 201. ✅ No change needed.

2. **Check for WebM video file availability:**
   Run: `ls -la public/videos/*.webm 2>/dev/null`
   - **If WebM file exists:** Add a `<source>` tag for WebM BEFORE the MP4 source in both `VideoBackground.tsx` and `HeroFallback.tsx`. The browser picks the first compatible source, so WebM first gives Firefox/Chrome the smaller file:
     ```tsx
     <source src="/videos/animation.webm" type="video/webm" />
     <source src="/videos/animation.mp4" type="video/mp4" />
     ```
   - **If WebM file does NOT exist:** Do not add WebM source tags. The `.mp4` path works in all browsers. Document in SUMMARY that WebM was not available and is a future optimization opportunity. Do NOT create a WebM conversion task — that's out of Phase 8 scope.

3. **Audit for console statements:**
   Run: `grep -rn "console\.\(log\|warn\|error\|debug\|info\)" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules`
   - Status from pre-investigation: Zero results. ✅ No change needed.
   - If any are found: remove them (they're debug artifacts that shouldn't ship).

4. **Audit for TODO/FIXME comments:**
   Run: `grep -rn "TODO\|FIXME\|HACK\|XXX" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules`
   - Status from pre-investigation: Zero results. ✅ No change needed.
   - If any are found in hero-related files: resolve or remove them.

5. **Final build validation:**
   Run: `bun run lint && bun run build`
   Both must pass with zero errors. Note any remaining lint warnings in the SUMMARY for future reference.
  </action>
  <verify>
    <automated>cd /home/jeandias/projects/penflow77 && grep -c "webkit-backdrop-filter" src/app/globals.css && grep -rn "console\.\(log\|warn\|error\)" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | wc -l && bun run lint && bun run build</automated>
  </verify>
  <done>
- Safari webkit prefixes confirmed present for all backdrop-filter declarations
- WebM source added to video elements (if WebM file exists) or documented as unavailable
- Zero console.log/warn/error statements in shipped source
- Zero TODO/FIXME/HACK/XXX comments in shipped source
- Lint and build pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual QA at four viewport widths</name>
  <files>none (visual verification only)</files>
  <action>
Start the dev server (`bun run dev`) and perform visual verification at four viewport widths using Chrome DevTools device toolbar (Ctrl+Shift+M).

What was built: Complete Phase 8 optimization — compressed GLB (~50-75% smaller), lazy-loaded below-fold section, dead code removed from HeroScene, per-frame FOV optimization, cross-browser audit passed. The hero section should now be ship-ready.

**Check each viewport:**

### 1. 375px (iPhone SE — mobile)
- [ ] HeroMobile renders with CTA-first stacked layout
- [ ] Text does not overflow or clip
- [ ] Video background visible (no blank areas)
- [ ] CTA buttons are tappable size (≥44px touch target)
- [ ] No horizontal scroll (page fits within 375px)

### 2. 768px (iPad — tablet)
- [ ] HeroContent renders (not HeroMobile)
- [ ] 3D pen model renders and floats
- [ ] Glass panel / backdrop-filter effects visible
- [ ] Text hierarchy is readable

### 3. 1024px (laptop)
- [ ] Full desktop experience: 3D pen, pointer parallax, scroll narrative
- [ ] Content layout is balanced (text + 3D canvas)
- [ ] Scroll down: pen rotates, content evolves
- [ ] No seams or gaps at viewport edges

### 4. 1440px (desktop)
- [ ] Canvas fills without over-scaling
- [ ] Gradient transitions smooth across full width
- [ ] CTA hover states feel premium (not janky)
- [ ] Typography is crisp — no blurry text from DPR mismatch

### Cross-cutting checks:
- [ ] Pen model loads without visual artifacts (meshopt compression didn't corrupt geometry)
- [ ] Reduced motion: enable `prefers-reduced-motion: reduce` in DevTools → all animations stop, pen is static, no layout shift
- [ ] Scroll cue appears on desktop, hidden on mobile
- [ ] No console errors in DevTools
  </action>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Human has verified hero renders correctly at all four viewports (375px, 768px, 1024px, 1440px), compressed pen model has no artifacts, and reduced motion mode works. User typed "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan is a QA audit with potential minor fixes (WebM source tags).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | N/A | N/A | accept | No new network surfaces, auth paths, or user data handling in this plan |
</threat_model>

<verification>
```bash
# Full build pipeline
bun run lint && bun run build

# Confirm no debug artifacts
grep -rn "console\.\(log\|warn\|error\)" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | wc -l

# Confirm webkit prefixes present
grep -c "webkit-backdrop-filter" src/app/globals.css
```
</verification>

<success_criteria>
- [ ] Safari `-webkit-backdrop-filter` prefixes confirmed present
- [ ] WebM video source added (if file exists) or documented as future optimization
- [ ] Zero `console.*` statements in shipped source
- [ ] Zero `TODO`/`FIXME` comments in shipped hero source
- [ ] `bun run lint` passes
- [ ] `bun run build` succeeds with zero TypeScript errors
- [ ] Human verification: hero renders correctly at 375px, 768px, 1024px, 1440px
- [ ] Human verification: compressed pen model has no visual artifacts
- [ ] Human verification: reduced motion mode works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/08-optimization-and-polish/08-03-SUMMARY.md`
</output>
