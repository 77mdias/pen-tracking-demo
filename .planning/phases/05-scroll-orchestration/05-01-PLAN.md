# Plan: useHeroScrollProgress — Scroll Progress Hook
**Phase:** 05 — Scroll Orchestration
**Plan:** 05-01

## Goal
Audit and confirm that `useHeroScrollProgress` is complete and correctly feeds normalized scroll progress (0–1) to the GSAP ticker loop. No executor work needed.

## Status
✅ **Complete (brownfield)**

## Audit Evidence

**File:** `src/hooks/useHeroScrollProgress.ts`

The hook is fully implemented and correct:

- Uses `ScrollTrigger.create` with `trigger: scope.current`, `start: "top top"`, `end: "bottom top"`, `scrub: true`
- Writes `self.progress` (0–1) into `progressRef.current` on every scroll update
- Returns `progressRef` (a stable `{ current: number }` ref) for use in the GSAP ticker
- Guards against `reducedMotion` by holding progress at `0` (no animation)
- Properly cleans up: `return () => scrollTrigger.kill()` inside `useGSAP`
- Dependencies: `[reducedMotion]` with `revertOnUpdate: true` — correct

No regressions, no TODOs, no dead code. **No changes required.**

## Threat Model
None — read-only side-effect hook. No user input, no external data.
