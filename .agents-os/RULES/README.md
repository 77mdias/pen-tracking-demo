# RULES (Deprecated)

Rule authority now lives in:
- root `AGENTS.md`
- local `AGENTS.md` files in scoped directories

This folder is intentionally minimized to avoid duplicated or conflicting rule sources.
